<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <form action="/campgrounds/<%=k._id%>?_method=PUT" method="POST">
        <div>
            <label for="title">title</label>
            <input type="text" id="title" name="campground[title]" value="<%=k.title%>">
        </div>
        <div>
            <label for="location">location</label>
            <input type="text" id="location" name="campground[location]" value="<%=k.location%>">
        </div>
        <button>Update</button>
        <footer>
            <a href="/campgrounds/<%=k._id %>">Back</a>
        </footer>
    </form>
</body>
</html>